services:
  inference:
    container_name: inference
    build: .
    ports:
      - "6969:6969"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]